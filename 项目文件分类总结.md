# RAGku 项目文件分类总结

## 📁 项目概述
RAGku 是一个基于 FastAPI 的 RAG（检索增强生成）系统，支持文件上传、向量化和网页聊天功能，已扩展至多模态场景（图片、视频分析）。

---

## 一、核心代码文件

### 1.1 主程序入口
| 文件名 | 说明 |
|--------|------|
| `app.py` | FastAPI 主应用入口 |
| `main.py` | 程序主入口 |
| `launcher.py` | 启动器逻辑 |

### 1.2 RAG 核心模块
| 文件名 | 说明 |
|--------|------|
| `rag_service.py` | RAG 服务核心逻辑 |
| `ingest.py` | 文档摄取/处理模块 |
| `document_extract.py` | 文档内容提取 |
| `vectorization_queue.py` | 向量化队列管理 |
| `upload_api.py` | 上传接口 API |
| `upload_persistence.py` | 上传持久化存储 |
| `chunked_upload.py` | 分块上传处理 |

### 1.3 AI/模型相关
| 文件名 | 说明 |
|--------|------|
| `bailian_ocr.py` | 百炼 OCR 集成（图片/视频分析） |
| `config.py` | 配置文件管理 |

### 1.4 工具脚本
| 文件名 | 说明 |
|--------|------|
| `simple_performance.py` | 性能监控 |
| `create_pptx.py` | PPT 生成脚本 |
| `screenshot_slides.py` | 幻灯片截图工具 |

---

## 二、前端项目 (frontend/)

### 2.1 前端结构
```
frontend/
├── dist/              # 构建输出
├── node_modules/      # 依赖包
├── public/            # 静态资源
│   └── assets/images/posters/pages/  # 海报图片 page-01.png ~ page-13.png
├── scripts/           # 构建脚本
│   └── build-slides.js
├── src/               # 源代码
│   ├── main.js        # 入口文件
│   ├── js/            # JS 模块
│   │   ├── ppt-controller.js
│   │   └── route-handler.js
│   ├── slides/        # 幻灯片组件 (slide-1.js ~ slide-19.js)
│   └── styles/        # CSS 样式
└── index.html
```

### 2.2 幻灯片页面
共 19 个幻灯片组件：`slide-1.js` 到 `slide-19.js`

---

## 三、文档资料 (docs/)

| 文件路径 | 说明 |
|----------|------|
| `docs/product/chapters.md` | 章节内容 |
| `docs/product/features.md` | 功能特性说明 |
| `docs/product/material.md` | 素材说明 |
| `docs/page-global-config.json` | 页面全局配置 |

---

## 四、演示与展示文件

| 文件名 | 说明 |
|--------|------|
| `RAG项目展示.pptx` | 项目演示 PPT |

---

## 五、启动脚本与批处理

### 5.1 Windows 批处理/CMD
| 文件名 | 说明 |
|--------|------|
| `01_一键配置密钥.cmd` | 配置 API 密钥 |
| `02_重建索引.cmd` | 重建向量索引 |
| `03_启动网页.cmd` | 启动网页界面 |
| `04_启动命令行.cmd` | 启动命令行模式 |
| `RAG启动器.cmd` | RAG 启动器 |
| `启动GUI.bat` | 启动 GUI |
| `重新打包.bat` | 重新打包程序 |

### 5.2 VBScript 脚本
| 文件名 | 说明 |
|--------|------|
| `创建桌面快捷方式.vbs` | 创建桌面快捷方式 |
| `启动GUI.vbs` | 启动 GUI |
| `启动RAG.vbs` | 启动 RAG |

---

## 六、打包与部署 (dist/)

| 目录/文件 | 说明 |
|-----------|------|
| `dist/RAG启动器/` | 打包后的可执行程序（含 _internal 依赖）|
| `dist/RAG启动器.exe` | 主启动器可执行文件 |
| `RAG启动器.spec` | PyInstaller 打包配置 |

---

## 七、数据与存储

### 7.1 数据目录 (data/)
| 目录/文件 | 说明 |
|-----------|------|
| `data/backups/` | 数据备份 |
| `data/vectorization_queue.db` | 向量化队列数据库 |

### 7.2 向量索引
| 文件名 | 说明 |
|--------|------|
| `index_meta.npy` | 索引元数据 |
| `index_store.npy` | 向量索引存储 |

### 7.3 上传文件 (uploads/)
| 目录/文件 | 说明 |
|-----------|------|
| `uploads/*.docx` | 用户上传的 Word 文档 |
| `uploads/knowledge_bases/` | 知识库文件 |
| `uploads/temp/chunks/` | 临时分块文件 |

### 7.4 知识库 (knowledge/)
| 说明 |
|------|
| 空目录，用于存储知识库文件 |

---

## 八、项目配置与依赖

| 文件名 | 说明 |
|--------|------|
| `requirements.txt` | Python 依赖包列表 |
| `README.md` | 项目说明文档 |

---

## 九、备份目录 (backup_performance/)

性能监控备份数据存储目录。

---

## 📦 文件分类统计

| 类别 | 主要文件类型 | 数量估算 |
|------|-------------|---------|
| 核心代码 | .py | ~15 个 |
| 前端代码 | .js, .ts, .css, .html | ~3760 个 |
| 文档资料 | .md, .json | ~4 个 |
| 启动脚本 | .cmd, .bat, .vbs | ~9 个 |
| 演示文件 | .pptx | 1 个 |
| 打包文件 | .exe, 依赖库 | ~5780 个 |
| 数据文件 | .npy, .db, .docx | 若干 |

---

## 🔗 百度网盘上传建议

根据截图中的表单，建议按以下方式组织：

1. **作品主文件夹** - 整个 ragku 项目
2. **作品文件夹** - 核心代码 + 前端
3. **作品代码文件夹** - Python 后端代码
4. **作品文档文件夹** - docs/ 目录 + README.md
5. **作品演示文件夹** - RAG项目展示.pptx + 前端海报图片
6. **作品部署链接** - dist/RAG启动器/ 打包程序

---

*生成时间：2026-04-19*
